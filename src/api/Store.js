import RiotAPI from "./RiotAPI";

export default class Store extends RiotAPI {
    constructor(username, password, lang) {
        super(username, password, lang);

    }
    // define store and add types of store inside of it 
    
    
}